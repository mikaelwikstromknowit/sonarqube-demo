#!/bin/bash

docker run --rm \
    --network bridge \
    -e SONAR_HOST_URL="http://$SONAR_HOST_IP:9000" \
    -v "${PWD}:/usr/src" \
    sonarsource/sonar-scanner-cli \
    -Dsonar.projectKey=$SONAR_PROJ_KEY \
    -Dsonar.login=$SONAR_TOKEN \
